# v0.8 trial findings — homelab-status-mcp on /Volumes/SneakerNet

**Date:** 2026-05-17.
**Trial repo:** `homelab-status-mcp` (Go MCP server, ~30 src files, baseline SHA `667b7f3`).
**Trial location:** `/Volumes/SneakerNet/bosun-trial/homelab-status-mcp` — relocated from `~/Documents/Homelab/` mid-trial after the iCloud-induced SIGBUS surfaced.
**Bosun version:** built from `main` at `43b4bf0` (v0.8 lane 1+2+3+4 + bug-hunt fixes).
**System load at start:** 1-min avg 5.94, dropped to ~4-5 through agent work.

---

## Outcome

**SUCCESS (after relocation).** Phase 1 (init), Phase 2 (parallel work), Phase 3 (resilience gates) all completed without safety-contract violation. The trial took two attempts:

1. **Attempt 1** at `~/Documents/Homelab/homelab-status-mcp` — failed at Phase 1 with SIGBUS on `git worktree add` (exit 138, "reset died of signal 10"). Root cause: iCloud File Provider concurrently writing to git's object store during checkout's mmap read. **Safety contract held under the failure** — trial repo HEAD unchanged, no half-state worktree dirs, `--resume` worked and continued from the breadcrumb.
2. **Attempt 2** at `/Volumes/SneakerNet/bosun-trial/homelab-status-mcp` (outside iCloud's scope) — clean Phase 1 + Phase 2 + Phase 3.

The fix that unblocked the trial was exactly the one `bosun doctor` recommended on attempt 1's preflight: get the repo out of `~/Documents/`.

---

## Doctor output (both locations)

**Attempt 1 (iCloud location):**

```
⚠ filesync-icloud: repository is under ~/Documents; macOS may sync it to iCloud (creates phantom files)
⚠ init-lock: init.lock is 5h16m0s old (prior init may have died)
(rest: PASS)
```

**Attempt 2 (SneakerNet location):**

```
All checks passed.
```

Diagnostic worked exactly as intended — doctor told us what the problem was on attempt 1; relocation cleared the warning on attempt 2.

---

## Phase 1 — Init

`bosun init 3 --brief v0.6-trial-plan.md` on `/Volumes/SneakerNet/...`:

```
system load is 6.52; init may be slow (--no-load-check to skip)
Creating worktree session-1 (1/3)...
Creating worktree session-2 (2/3)...
Creating worktree session-3 (3/3)...
Created 3 session(s):
  session-1  → /Volumes/SneakerNet/bosun-trial/homelab-status-mcp-bosun-1  (branch: bosun/session-1)
  session-2  → /Volumes/SneakerNet/bosun-trial/homelab-status-mcp-bosun-2  (branch: bosun/session-2)
  session-3  → /Volumes/SneakerNet/bosun-trial/homelab-status-mcp-bosun-3  (branch: bosun/session-3)
```

Post-init state verification:

- HEAD `667b7f3` (unchanged from pre-trial)
- Working tree diff: only `.gitignore` (bosun auto-adds `.bosun/`)
- `git worktree list`: 3 worktrees registered under `bosun/session-N` branches, all at base SHA
- `bosun status`: 3 WORKING, 0 dirty, 0 claimed (no stale-claim bleed from prior round)

---

## Phase 2 — Multi-session round

(filled in as the trial progresses)

### Launch

```
~/go/bin/bosun launch session-1   # default brief prompt via v0.7 lane 1
~/go/bin/bosun launch session-2
~/go/bin/bosun launch session-3
```

All three launched cleanly. MCP daemon spawned once and reused across launches. RUNNING column correctly identified PIDs via the v0.7 cmdline-fallback fix (the macOS `~/.local/share/claude/versions/<X.Y.Z>/` install path that previously broke proc detection).

### Session outcomes

(TBD — agents in flight as of this writing)

---

## Phase 3 — Resilience gates

(TBD)

- [ ] **Live-agent merge refusal** — try `bosun merge session-1` while session-1 is mid-edit, expect refusal naming the PID
- [ ] **CRASHED + rescue** — force-close one session's Ghostty window, observe state transition, `bosun rescue` snapshots dirty files
- [ ] **Corrupted gitdir recovery** — manually `rm` one worktree's `.git/worktrees/<name>/HEAD`, run `bosun rescue`, expect clean refusal with a recovery hint
- [ ] **merge --undo** — squash-merge one session, then `merge --undo`, verify main resets to pre-merge SHA via reflog

---

## Findings

### Procedural / environmental (outside bosun's control, but documented for operators)

1. **Trial repo recovery required manual git surgery.** A prior aborted run left the repo with a corrupted `internal/parsers/testdata/uptime_typical.txt` and a stale `.git/index.lock`. `bosun doctor` caught some of the leftover state (stale `.bosun/init.lock`) but not the git-level corruption. Recovery: `rm` the corrupted file → `rm .git/index.lock` → `git checkout HEAD -- <file>`. Operator can dispatch this from doctor's output but doctor doesn't autofix.

2. **`mv` across volumes is `cp` + `rm`.** Moving a 15M repo across volumes via `mv` invoked `fcopyfile` for each git object; multiple files hit "Operation timed out" partway through. Caused by iCloud's File Provider concurrently virtualizing the source files (`ditto` also failed with phantom "No such file or directory" errors on files that demonstrably existed). **The reliable approach was `tar | tar` piping** — single I/O stream, no per-file fcopyfile, transparent to iCloud's lazy-hydration.

3. **macOS Sequoia ships `openrsync`, not GNU rsync.** Flags like `--info=progress2` are silently rejected (rsync prints its usage and exits 0). `--partial` works but the progress UX is absent. Worth flagging in any doc that recommends rsync.

4. **Dock-attached external M.2 drives are slow under load.** The drive itself is APFS/journaled (fine), but the USB controller in the dock dropped reads under concurrent I/O. Single-stream copy (`tar | tar`) worked; many-small-file copy (`ditto`) didn't. The user offered to plug the drive direct to MacBook — worth doing for the trial run itself, not just the copy.

### Bosun-side findings

5. **Safety contract held under SIGBUS.** Attempt 1's init failed mid-`git worktree add` checkout. Bosun emitted a clear error naming the operation + exit code 138, left the trial repo's HEAD unchanged, did not create any half-state worktree dirs, and wrote an `init.state` breadcrumb. `bosun init --resume` correctly picked up the breadcrumb on retry. **This is the load-bearing validation for the public-launch gate** — when bosun fails because of an environmental hazard, the failure is clean.

6. **Doctor's iCloud-sync warning is load-bearing operator guidance.** On attempt 1, doctor identified the exact failure cause before init even ran. The recommendation ("move the repository to ~/code/ or similar") was correct. **Doctor's value is fully validated.**

7. **`bosun init --suggest` not yet exercised in trial** — the brief was hand-written. Worth a future trial that exercises the `--suggest` path against a fresh external repo.

### Trial #1 → trial #2 deltas

Trial #1 (2026-05-16) blocked at Phase 1 with two bugs:
- Silent init hang at `git worktree add` (no timeout fire). **Fixed v0.6.1.** Verified clean this trial.
- Broken `--resume` (arg-count mismatch + refusal on locked worktree). **Fixed v0.6.1.** Verified clean this trial.

Trial #1.5 (2026-05-17 morning) blocked at Phase 1 again with SIGBUS under crushing load. **Worked around via the iCloud relocation here; v0.8 doctor surfaces the root cause proactively now.**

---

## v0.9 candidates surfaced

(non-safety, worth deferring until after launch)

- **UID-per-worktree dir naming** (already captured as a separate task). Would have eliminated the "worktree path already exists" friction we kept seeing across rounds.
- **`bosun doctor --fix` opt-in autofix** for the safe-to-touch issues: drop `.metadata_never_index`, clean stale init.lock if older than X, rename orphan worktree dirs to `_orphan-<name>`. Roadmap open question #1 — answer based on this trial's experience.
- **Doctor recommend the relocation explicitly.** Current message is "either disable iCloud sync ... or move the repository to ~/code/ or similar." Could be more concrete: name a target dir (`~/code/`, `/tmp/`, or "/Volumes/<external>/"), generate the `mv` command for copy-paste.
- **Onboarding doc on macOS environmental hazards.** A `docs/macos-setup.md` covering iCloud, the relocation patterns, and how to verify via doctor. Lowers the bar for first-time users.

---

## Verdict for the v0.8 public launch

(filled in after Phase 2 + 3 complete)
