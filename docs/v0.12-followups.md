# v0.12 follow-ups — Low/Medium items deferred from the 2026-05-21 loop

Captured during the iterative bug-hunt / security loop on 2026-05-21.
Each loop iteration scans for **High/Critical only** and fixes them
inline; anything Low or Medium lands here for a dedicated polish pass.

Source audits:
- `docs/security-audit-2026-05.md` (M1–M7, L1–L4)
- `docs/bug-hunt-2026-05.md` + `docs/bug-hunt-2026-05-pass2.md` (all closed in fc2b6e6)
- 2026-05-21 loop iteration #1 — found 1 High (spawn quota TOCTOU, fixed)

## Medium

### M1 — Brief size in `bosun_spawn` is unbounded — **FIXED 2026-05-21 (Bundle A)**

**Where:** `internal/mcp/tool_spawn.go` — `BriefMarkdown` arg had no size cap.
**Resolution:** `maxBriefBytes = 256 << 10` constant + refusal at the toolSpawn gate before any allocation downstream. Refused requests audit under `spawnGateInvalidArgs`. Test: `TestSpawn_OversizedBriefRefuses`.

### M2 — Anthropic API response has no size limit — **FIXED 2026-05-21 (Bundle A)**

**Where:** `internal/suggest/claude.go` — `raw, err := io.ReadAll(resp.Body)`.
**Resolution:** Wrapped in `io.LimitReader(resp.Body, maxAnthropicResponseBytes)` (10 MiB). Compromised/proxied endpoint can no longer drive unbounded allocator pressure.

### M3 — TOCTOU race in `bosun_spawn` concurrent quota — **FIXED 2026-05-21**

**Where:** `internal/mcp/tool_spawn.go:142-148`, `internal/spawn/spawn.go:150`.
**Resolution:** `spawntree.AddChildIfUnderQuota` does count+check+add atomically inside the flock; `spawn.Run` uses it. Concurrent + multi-brief callers can no longer bypass `agent_spawn.max_concurrent_sub_sessions`. Test: `TestAddChildIfUnderQuota_ConcurrentRespectsCap`.

### M4 — History archive files written with mode `0o644` — **FIXED 2026-05-21 (Bundle A)**

**Where:** `internal/history/history.go` — `brief.md`, `claims.json`, `commits.log`, `merged.txt`, `metadata.json`.
**Resolution:** All `os.WriteFile` calls now use `0o600`. `copyFileIfExists` switched from `os.Create` (0o666 + umask) to `os.OpenFile(..., 0o600)` so `brief.md` and `claims.json` also land 0o600. Test: `TestArchive_CapturesBriefClaimsCommits` now asserts each archive file's mode.

### M5 — Lock acquisition has no timeout — **FIXED 2026-05-21 (Bundle B)**

**Where:** `internal/lockfile/lockfile_unix.go` + `lockfile_windows.go` — both used blocking acquire.
**Resolution:** Replaced blocking `LOCK_EX` / `LockFileEx` calls with non-blocking equivalents (`LOCK_EX|LOCK_NB` on Unix, `LOCKFILE_FAIL_IMMEDIATELY|LOCKFILE_EXCLUSIVE_LOCK` on Windows) inside a poll loop bounded by `DefaultTimeout = 30s`. Each successful acquire stamps `pid=N ts=<rfc3339>` into the lock file inside the locked section; on timeout the waiter reads that diagnostic data back and returns `*LockTimeoutError{Path, Timeout, HolderPID, HeldFor}`. The error unwraps to `ErrLockTimeout` for sentinel-style checks. Tests: `TestWithLock_TimeoutSurfacesLockTimeoutError` (real holder + contention), `TestWithLock_TimeoutErrorMessage` (formatter shape).

### M6 — HTTP serve missing security headers + body size limit — **FIXED 2026-05-21 (Bundle C)**

**Where:** `internal/web/server.go`.
**Resolution:** New `securityHeaders` middleware wraps the dashboard mux and emits `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: no-referrer`, and a strict CSP (`default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self'; base-uri 'none'; frame-ancestors 'none'`). `'unsafe-inline'` is needed because the embedded `index.html` carries inline `<style>` + `<script>`; everything else is locked to `'self'`. `http.Server.MaxHeaderBytes = 1 MiB` (explicit, pinned against future stdlib defaults). `http.MaxBytesReader(w, r.Body, 1 MiB)` applied per-request in the middleware (defense-in-depth — current handlers are GET-only). Test: `TestServer_SecurityHeaders` asserts every header on both an HTML route and a JSON route.

### M7 — PIPE_BUF assumption on macOS for usage lines — **FIXED 2026-05-21 (Bundle D)**

**Where:** `internal/usage/usage.go`.
**Scope correction:** The original audit named `spawn_audit.go` and `subtask_audit.go` too, but on inspection both already wrap their writes in `lockfile.WithLock` — they're protected by the flock and don't rely on PIPE_BUF atomicity. Only `usage.go` appended unlocked under the POSIX atomic-append assumption.
**Resolution:** New `encodeLineUnderCap` helper marshals the entry, and if the line would exceed `maxLineBytes = 480` (under macOS PIPE_BUF=512 with 32-byte headroom for JSON escape inflation + newline), iteratively shrinks `TurnLabel` then `Model` to a `minFieldLen = 32` floor and re-marshals. If still over after both fields are at the floor, returns an error rather than silently dropping the entry. `Append` now routes through it; numeric fields (TokensIn/Out, CostUSD, Timestamp) are never touched. Tests: `TestAppend_TruncatesOversizedFieldsForPIPEBUF` (100KB Model + 100KB TurnLabel still produces a sub-cap on-disk line); `TestEncodeLineUnderCap_NormalEntryUnchanged` (regression: ordinary entries round-trip with no mutation). The old `TestRead_HandlesAtypicallyLongLine` was replaced — it had asserted huge labels round-tripped, which is exactly the contract that's now reversed.

## Low

### L1 — Anthropic 5xx error body logged verbatim — **FIXED 2026-05-21 (Bundle A)**

**Where:** `internal/suggest/claude.go` — non-2xx path's `strings.TrimSpace(string(raw))` fallback.
**Resolution:** The non-2xx fallback no longer includes the raw body. If `apiErr.Error.Message` parses, we surface that; otherwise we surface only the status code with a note that the response had no parseable `error.message`. Decode-failure path on 2xx (separate concern) intentionally retains its 200-char truncated body since that's a debugging aid for malformed-but-legitimate responses.

### L2 — `bosun_attach` allows arbitrary PIDs — **FIXED 2026-05-21 (Bundle E)**

**Where:** `internal/mcp/tool_attach.go`.
**Resolution:** Two layers added in `toolAttach`:
1. **PID 1 hard refuse** — init/launchd on every supported platform; never a real bosun worker, and `proc.IsAlive` can't disprove PID 1 so the false-positive would be permanent. Error message names it explicitly.
2. **Best-effort cwd validation** — new `proc.Cwd(pid)` returns `/proc/<pid>/cwd` on Linux and `ErrCwdUnsupported` on macOS/Windows (where exposing per-process cwd cleanly would need cgo or new third-party deps). The handler calls it via a `pidCwdFn` indirection on the Server (test-overridable, matches existing `runningFn` pattern). On Linux: refuses if the cwd doesn't descend from `sess.Path` (with EvalSymlinks fallback to handle `/var/folders` ↔ `/private/var/folders` on macOS). On macOS/Windows: `ErrCwdUnsupported` falls through silently — best-effort by design.

Helper: `cwdInsideWorktree` does the prefix match with a trailing-separator guard so `session-1` ≠ `session-10`. Tests: `TestCwdInsideWorktree` (table-driven over 7 path shapes); `TestServer_AttachToolCwdValidationRefusesMismatch` (stubbed pidCwdFn returns an outside-the-worktree cwd, handler refuses, attached-pid file not written); `TestServer_AttachToolCwdValidationSkipsWhenUnsupported` (stubbed `ErrCwdUnsupported` falls through; attached-pid is written); PID 1 case added to existing `TestServer_AttachToolInvalidPIDRefuses` table.

### L3 — Custom MCP tools inherit the bosun process env — **FIXED 2026-05-21 (Bundle E)**

**Where:** `internal/mcp/tools_custom.go`, `internal/config/config.go` (MCPToolDef doc comment).
**Resolution:** Doc-only as planned. Extended the `MCPToolDef` Security model docstring with a paragraph spelling out env inheritance and the operator-side mitigation (wrap the tool in an env-scrubbing script if it hands argv to an untrusted subprocess). Mirrored the note at the `exec.CommandContext` call site in `tools_custom.go` so anyone reading the execution code sees the same context without round-tripping to config docs. No code change to the exec path itself — env inheritance is intentional because most useful custom tools need the credentials.

### L4 — `BOSUN_BRIEF.md` not in repo's `.gitignore` — **FIXED 2026-05-21 (Bundle A)**

**Where:** repo-root `.gitignore`.
**Resolution:** Added `BOSUN_BRIEF.md` with comment explaining the per-session brief may contain inline operator context. Note: each session worktree already gets a `.git/info/exclude` entry for `BOSUN_BRIEF.md` (set by cmd_init / spawn.Run); the root `.gitignore` is belt-and-suspenders for the main worktree case.

## Notes — already verified safe (don't re-flag)

See `docs/security-audit-2026-05.md` § NOTES for the full list:
- **N1** — `agent_command` shell interpolation is by design (operator-edited config, not third-party input).
- **N2** — `removeIfSocket` uses `os.Lstat` + `ModeSocket` check; safe against symlink races.
- **N3** — `cmd_remove`'s `os.RemoveAll` trusts `git worktree list`; tampering already implies repo control.

## Recommended pass shape (when picked up)

These items are bounded scope and well-localized:
- **Bundle A** — input caps + file modes + log scrubbing + .gitignore (M1, M2, M4, L1, L4). **DONE 2026-05-21.**
- **Bundle B** — lockfile timeout (M5). **DONE 2026-05-21.**
- **Bundle C** — HTTP security headers + body cap (M6). **DONE 2026-05-21.**
- **Bundle D** — PIPE_BUF cap for usage writes (M7). **DONE 2026-05-21.**
- **Bundle E** — bosun_attach PID validation + custom-tools env doc (L2, L3). **DONE 2026-05-21.**

**All 12 followups (M1–M7, L1–L4) are now closed.**

## Post-trial Windows findings (2026-05-22 evening)

The VNC-based Windows trial (see `docs/windows-trial-2026-05-22.md`)
surfaced two new findings worth filing for a future Windows pass:

### W1 — `cmd.exe` launcher fallback: cd + agent_command chain silently mis-quotes — **Medium**

**Where:** `internal/launcher/launcher.go` `cmdExeArgs`.
**Risk:** When `wt.exe` is unavailable (Win10 without Windows Terminal install), bosun's fallback constructs `cmd /c start "" cmd /K cd /D "<worktree>" && <agent_command>` as a single argv element. cmd.exe's documented quote-handling rule (`cmd /?` § /K) strips the outer quotes when the argument has >2 quotes plus special chars (`&&`), leaving the `\"<path>\"` as literal backslash-quotes. The launched window opens (so naive testing says "works") but cds to drive root, then fails to find `<agent_command>`. **Verified empirically** during the 2026-05-22 trial — zero `launched-marker.txt` files appeared anywhere on the filesystem after a fallback run.
**Fix path:** write a per-launch `.bosun/launch-<session>.cmd` helper inside the worktree at launch time and invoke it via `cmd /K <path-to-helper>` — no in-argv quoting to fight. Mirrors the marker.cmd pattern the trial proved works. Alternative: refuse to fall back to cmd.exe and surface "Windows Terminal required" with an install hint.

### W2 — `bosun cleanup --force` doesn't name the blocker on Windows — **Low**

**Where:** `cmd/bosun/cmd_cleanup.go` (error wrapping path).
**Risk:** Windows refuses to delete a worktree dir any process has open as cwd. Launched terminal windows hold their worktree cwd open, so `bosun cleanup` after `bosun init --launch` surfaces a bare `git worktree remove …: exit status 255: Permission denied`. Operators on macOS/Linux never see this because POSIX allows the unlink. Operators on Windows hit a dead-end error with no actionable next step.
**Fix path:** when the inner git error matches the Windows-shape "Permission denied" + "failed to delete", walk `Win32_Process` for cmd/conhost processes whose CommandLine references the worktree path, surface them in the error ("worktree pinned by PID 1234 (cmd.exe) — close the launched terminal and retry"). Aggressive option behind `--terminate-blockers`: kill them first then retry the remove.
